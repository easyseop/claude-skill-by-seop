# om-verify SKILL.md 한글화 재빌드 지시서

작성일: 2026-08-25. 배경: 사람 결정 — **스킬 본문 언어를 한글로 통일**(om-plan은 이미 한글, om-apply는 Codex 단계에서 한글화). 이 작업은 1차 검토 **승인을 받은** om-verify 저작 결과의 언어 변경 재빌드다 — 내용·경계·구조는 검증본과 동일하게 유지하고 언어만 바꾼다.

실행 위치: `~/claude-skill-by-seop`. 메타스킬 `claude-skill-author` 설치·검증 완료 상태.

## 안전 원칙 (기존과 동일)

- 커밋·푸시 금지. 참고 자료 저장소 2곳 읽기 전용(검사기·skill_develop — 경로는 `검토요청_om-verify_저작결과_20260825.md` §3).
- 쓰기는 `.claude/skills/om-verify/`·`.claude/skill-authoring/om-verify/`·`docs/om-verify/`에만.
- om-plan 경로는 읽지도 판정하지도 않는다(병렬/후속 작업 중).

## 작업 (메타스킬 build→audit 단계 재수행)

1. **결정 반영**: `RULE_COVERAGE.yaml`의 `D-01`(규칙 문장 한글) 판정을 현재의 예외(영어) 근거에서 **APPLY(한글 본문)**로 바꾸고, rationale에 "사람 결정 2026-08-25: 한글 통일(사내 GitLab에서 한국어 조직이 직접 읽는 문서, om-plan과 일관). wiring 테스트 단언은 이식 시 한글 문구로(Codex 작업)"를 기록. `COMMAND_SPEC.yaml`·`SPEC_REVIEW.md`·`AUTHORING_REVIEW.md`의 언어 결정 기록(§9-1 관련)도 같은 취지로 갱신.
2. **SKILL.md 한글 재작성** (`.claude/skills/om-verify/SKILL.md`):
   - 현재 영어본(225줄, 1차 검토 승인본)의 **의미를 1:1로 보존**하며 한글로 다시 쓴다. 문장 추가·삭제·완화 금지 — 언어만 바꾼다.
   - 기술 토큰은 원문 유지: reason code(`VERIFY_*`), 파일·경로·스키마명, CLI 명령, 필드명(`run_id` 등), 상태값(`verified`/`failed`/`infra_error`, `skipped_due_to_stop`, `not_configured`), frontmatter의 `name`·`argument-hint` 값.
   - frontmatter `description`은 한글로(모델이 호출 판단에 쓰는 필드 — 검색성 위해 om-verify·verify 등 키워드 포함).
   - 참고: om-plan SKILL.md(한글)의 문체·용어를 따라 두 문서의 어조를 맞춘다(예: "~않는다/~금지", 게이트·marker·receipt 용어).
3. **앵커 재작성**: `RULE_COVERAGE.yaml`의 APPLY/TRANSFORM `targets[].contains` 70개를 새 한글 본문의 **실재 문구**로 전부 교체(의미상 같은 문장을 가리키게). 안티패턴 evidence 중 본문 문구 인용도 갱신.
4. **기계검증**: `validate_authoring.py --phase build` exit 0 (앵커 누락 0 확인 포함).
5. **새 독립 감사 1회**(새 인스턴스, 읽기 전용): 중점 = ① 영어본↔한글본 의미 등가(완화·누락 없음 — 특히 Non-negotiable boundary와 완료 정의) ② 앵커 70개가 규칙의 실질 의미를 여전히 구현 ③ 기술 토큰 원문 유지. **감사 원문은 `AUDIT_REPORT_ROUND4.md`로 옮긴 기존 4차 대신 새로 `AUDIT_REPORT.md`에 기록**(기존 4차 PASS 원문을 `AUDIT_REPORT_ROUND4.md`로 먼저 이동).
6. `--phase audit` exit 0 → `FINAL_STATUS.json` PASS 갱신 확인.
7. **결과 보고 파일**: `검토요청_om-verify_한글화_20260825.md` — 변경 요약·감사 판정·재현 명령(기존 §7과 동일 5종)·영어본 대비 의미 변경 0 주장과 그 근거. 커밋 금지.

## 근거 문서

- 1차 검토 승인: `검토결과_om-verify_1차_20260825.md` (코드 실측 재반증 14건 표 — 한글화 후에도 이 사실 주장들이 그대로 보존되어야 한다)
- 원 저작 인수인계: `검토요청_om-verify_저작결과_20260825.md`
