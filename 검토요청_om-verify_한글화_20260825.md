# 검토 요청 — om-verify SKILL.md 한글화 재빌드 결과

작성일: 2026-08-25. 작성자: Claude(재빌드 세션). 수신: 사람 · Codex 2차 검토.
근거 지시서: `지시서_om-verify_한글화_20260825.md`.
선행 문서: `검토요청_om-verify_저작결과_20260825.md`(원 저작 인수인계) · `검토결과_om-verify_1차_20260825.md`(영어본 1차 검토 **승인**).
저장소: `/Users/seop/claude-skill-by-seop` (미커밋).

---

## 1. 결론

- `FINAL_STATUS.json` = **`PASS`** (`2026-08-25T01:46:17Z`, 한글본 기준으로 갱신됨).
- 기계 검증 `build`·`audit` 종료코드 0. `audit_verdict: PASS`.
- 한글본 대상 독립 감사 2회: **5차 CONDITIONAL**(차단 1건) → 수정 → **6차 PASS**(차단 0건).
- 규칙 89개 전수 판정 유지. 설계 요구 18개 `RESOLVED` 유지.
- **최종 산출물의 영어본 대비 의미 변경: 0건.** 근거는 §4.
- 참고 자료 저장소(검사기) 쓰기 0건. **커밋·푸시 0건.**

---

## 2. 변경 요약

### 결정 반영 (지시서 §작업1)

| 항목 | 이전 | 이후 |
|---|---|---|
| `RULE_COVERAGE`의 `D-01` | `TRANSFORM` — "이식 대상이 영어이므로 본문은 영어" | **`APPLY`** — "규칙 문장은 한글로 작성한다" |
| 판정 집계 | `APPLY` 42 · `TRANSFORM` 23 | **`APPLY` 43 · `TRANSFORM` 22** (D-01 이동분) |
| `COMMAND_SPEC.open_questions`의 D-01 | 미결(사람·Codex가 뒤집을 수 있음) | **`[해소됨 2026-08-25]`** |
| `SPEC_REVIEW.md` 언어 결정 | "본문 영어, 증거 한글" | "본문 **한글**, 기술 토큰 원문 유지" |

`D-01` rationale에 기록한 사람 결정 문구: *"사람 결정 2026-08-25: 한글 통일(사내 GitLab에서 한국어 조직이 직접 읽는 문서, om-plan과 일관). wiring 테스트 단언은 이식 시 한글 문구로(Codex 작업)"*. 이전 판정의 근거는 지우지 않고 **"사람 결정으로 대체됐다"**고 남겼다.

### 본문 재작성 (지시서 §작업2)

- `.claude/skills/om-verify/SKILL.md` — **225줄(영어) → 197줄(한글)**. 파일은 여전히 이 하나뿐이다.
- 절 7개 · 절차 5단계 · 절대 경계 13항목 · 입력 계약 표 6행 · 케이스 표 3행 · 함정 표 4행 · 조건부 4항목 · 보고 템플릿 5절 — **개수와 순서가 영어본과 동일**.
- 줄 수 감소는 삭제가 아니라 줄바꿈 압축이다. 6차 감사가 영어본의 모든 의미 단위에 대응 문장이 있음을 개별 확인했다.
- **기술 토큰 원문 유지**: reason code(`VERIFY_*`), 파일·경로·스키마명, CLI 명령, 필드명(`run_id` 등), 상태값(`verified`/`failed`/`infra_error`·`skipped_due_to_stop`·`not_configured`·`local-issued`·`fresh`·`analysis_error`·`api-live`/`browser`/`source-static`), frontmatter의 `name`·`argument-hint` 값.
- `description`은 한글로 다시 썼고 `om-verify`·`verify` 검색 키워드를 포함했다.
- 문체는 om-plan SKILL.md(한글)에 맞췄다(`~한다`/`~하지 않는다`, 게이트·receipt 용어). 지시서 §작업2의 참고 지시에 따라 om-plan SKILL.md를 **문체 참고 목적으로만** 읽었고 판정·수정 대상으로 삼지 않았다.
- 부수 효과: 최종 보고 형식이 `COMMAND_SPEC.outputs.final_report_sections`(요약·수행 범위·결과와 근거·검증 결과·미확인 사항과 남은 위험)와 이제 문자 그대로 일치한다. 영어본에서는 같은 뜻의 영어 제목이라 표기가 갈렸다.

### 앵커 재작성 (지시서 §작업3)

- `RULE_COVERAGE`의 `APPLY`·`TRANSFORM` `targets[].contains` **71개를 전부 한글 실문구로 교체**. 6차 감사가 71개 전수 실재를 확인했고, 각각 **영어본에서 가리키던 것과 같은 문장**을 가리킨다.
- 지시서는 70개로 적었으나 실제는 71개다. `D-01`이 `APPLY`가 되면서 한글 본문 증거용 target 1개가 추가됐기 때문이다(정당한 증가, 문서화됨).
- 본문 문구를 인용하던 안티패턴 evidence 3건(`A-08`·`A-09`·`A-11`)도 한글 문구로 갱신했다.
- `planned_locations` 71건의 영어 절 이름도 한글 실제 제목으로 갱신했다(6차 감사 확인: 남은 영어 절 제목 0건).

### 산출물 목록

| 경로 | 상태 |
|---|---|
| `.claude/skills/om-verify/SKILL.md` | 한글 197줄. 이 폴더의 유일한 파일 |
| `docs/om-verify/OM_VERIFY_DESIGN.md` | 327줄. 설계서는 원래 한글이라 변경 없음 |
| `.claude/skill-authoring/om-verify/` | 17개. 아래 신규 3개 포함 |
| ├ `SKILL_EN_PREVIOUS.md` | **신규** — 직전 영어본 보존 사본(§5 참조) |
| ├ `AUDIT_REPORT_ROUND5.md` | **신규** — 한글본 5차 감사 원문(CONDITIONAL) |
| └ `AUDIT_REPORT.md` | **교체** — 한글본 6차 감사 원문(PASS). 이전 4차 원문은 `AUDIT_REPORT_ROUND4.md`로 이동 |

---

## 3. 감사 판정

| 회차 | 대상 | 판정 | 차단 | 비차단 |
|---|---|---|---|---|
| 1~4차 | 영어본 | PASS(3차만 CONDITIONAL 후 해소) | 0 | — |
| **5차** | 한글본 | **CONDITIONAL** | **1** | 9 |
| **6차** | 한글본 수정본 | **PASS** | **0** | 8 |

### 5차가 잡은 차단 1건 — 번역 중 행위 주체가 뒤바뀜

- **문제**: 영어본 `You write verify-request.json.`의 `You`는 스킬을 실행하는 에이전트인데, 한글본이 「`verify-request.json`은 **사용자가** 작성한다」로 옮겼다.
- **왜 차단인가**: (a) 같은 문서의 「이 명령은 **요청 작성**과 판독과 보고를 한다」와 정면 모순, (b) `allowed-tools`에 `Write`를 넣은 유일한 본문 근거가 사라져 `COMMAND_SPEC.permissions.allow`·`allowed_tools_policy`와 불일치, (c) 지시서가 승인한 범위("언어만")를 벗어난 의미 변경.
- **조치**: 「`verify-request.json`은 **이 명령이** 작성한다.」로 되돌렸다. 다른 문장은 손대지 않았다.
- **6차 확인**: 모순 두 가지가 모두 해소됐고, **수정이 과하지 않음**도 확인됐다(5차가 인용한 줄 번호 14개가 현재 파일에서 같은 문장을 가리키고, 앵커 줄 번호도 전부 일치 → 문제 문장 1줄만 제자리 치환). 정본 `83` 부록 A-1(「입력(사람/LLM 작성)」)과도 충돌하지 않는다.

### 반영한 비차단 지적

5차 4건(`SPEC_REVIEW` 규칙 집계 43/22 · `D-08` 사유 줄 수 197 · `planned_locations` 한글화 · 남은 위험 번호) + 6차 4건(감사 원문 파일 인용 정합 · 회차 표기 6회 · 번호 참조 11 · `SPEC_REVIEW` 스냅숏 시점 표기). 미반영 4건은 사유와 함께 `AUTHORING_REVIEW.md`에 기록했고 6차 감사가 전부 **미반영이 정당하다**고 판정했다.

---

## 4. "영어본 대비 의미 변경 0" 주장과 그 근거

**주장**: 현재 `SKILL.md`(한글 197줄)는 1차 검토 승인본인 영어 225줄과 의미가 등가다. 완화·누락·추가가 없다.

**정직한 단서**: 이 주장은 처음부터 참이었던 것이 아니다. 최초 한글 초안에 의미 변경 **1건**(§3의 행위 주체 뒤바뀜)이 있었고, 5차 감사가 잡아 수정한 뒤 6차 감사가 0건을 확인했다. 아래 근거는 **수정 후 현재 상태**에 대한 것이다.

| 근거 | 확인 주체 | 내용 |
|---|---|---|
| 문장 단위 대조 | 5차·6차 감사자(각각 새 인스턴스) | 「절대 경계」 13항목·완료 정의·조건부 4항목·절차 5단계·케이스 표 3행·함정 표 4행·보고 템플릿 5절을 영어본과 1:1 대조. 차이 0건 |
| 완화어 검사 | 5차·6차 | `가급적`·`필요하면`·`가능한 한` 등 완화어 **0건**. 조건 확대 **0건**. 항목 누락 **0건** |
| 종결형 등가 판정 | 5차·6차 | 영어 `never`/`only`/`always` → 한글 `~하지 않는다`/`~때만`/`항상`은 공통규칙 `C-07`이 규정한 종결형이므로 등가 |
| 구조 계수 | 5차·6차 | 절 7 · 절차 5 · 경계 13 · 표 6/3/4행 · 조건부 4 · 템플릿 5절 — 영어본과 전부 동일 |
| 앵커 동일성 | 6차 | 71개 target이 전부 실재하고, **영어본에서 가리키던 것과 같은 문장**을 가리킨다 |
| 기술 토큰 | 5차·6차 | reason code·스키마 경로·CLI·필드명·상태값·`name`·`argument-hint` 번역·변형 **0건** |
| 사실 주장 보존 | 6차 | 1차 검토의 **코드 실측 재반증 14건**이 한글본에 전부 대응 문장으로 살아 있음을 확인. 그중 8건은 6차가 엔진 코드로 재검증 |
| 수정 과잉 부재 | 6차 | 차단 수정 후에도 197줄 구조·줄 번호가 그대로 → 1줄만 제자리 치환 |
| 기계 검증 | 스크립트 | `--phase build` 종료코드 0(앵커 누락 0), `--phase audit` 종료코드 0 |

**이 주장의 한계는 §5에 있다.**

---

## 5. 이 재빌드가 만든 새 위험 (반드시 읽을 것)

**영어 기준본의 바이트 동일성이 증명되지 않았다.**

- `.claude/skills/om-verify/`는 **커밋된 적이 없어** 한글화로 덮어쓴 영어본을 Git에서 복원할 수 없다.
- 그래서 저작 세션이 한글화 직전 읽은 전문을 `.claude/skill-authoring/om-verify/SKILL_EN_PREVIOUS.md`에 옮겨 대조 기준으로 삼았다. 파일 머리말에 출처와 한계를 고지했다.
- **5차·6차 감사의 "의미 등가" 판정은 이 사본이 실제 영어본과 동일하다는 전제 위에 있다.** 사본이 누락·변형됐다면 등가 판정도 그 범위에서만 유효하다. 기계로 해소할 수 없는 한계다.
- 6차 감사자가 할 수 있었던 최대치는 4차 감사 원문(`AUDIT_REPORT_ROUND4.md`, 영어본 대상 PASS)이 인용한 문장들이 사본과 일치한다는 정황 확인이었다.
- **교훈**: 다음 재빌드 전에는 원본을 커밋하거나 별도 사본을 먼저 만들어야 한다.

---

## 6. 재현 명령

`검토요청_om-verify_저작결과_20260825.md` §7과 동일한 5종이다. 전부 읽기 전용이거나 증거 폴더에만 쓴다.

```bash
cd /Users/seop/claude-skill-by-seop

# 1) 기계 검증 3단 (각각 종료코드 0)
python3 .claude/skills/claude-skill-author/scripts/validate_authoring.py \
  --project-root "$(pwd)" --target om-verify --phase spec
python3 .claude/skills/claude-skill-author/scripts/validate_authoring.py \
  --project-root "$(pwd)" --target om-verify --phase build
python3 .claude/skills/claude-skill-author/scripts/validate_authoring.py \
  --project-root "$(pwd)" --target om-verify --phase audit

# 2) 설계서 digest 사슬 (3건 모두 True)
python3 -c "
import json,hashlib,pathlib
d=json.load(open('.claude/skill-authoring/om-verify/DESIGN_REQUIREMENTS.yaml'))
for x in d['source_documents']:
    print(hashlib.sha256(pathlib.Path(x['path']).read_bytes()).hexdigest()==x['sha256'], pathlib.Path(x['path']).name)
"

# 3) APPLY/TRANSFORM target 실재 (누락 0)
python3 -c "
import json,pathlib
cov=json.load(open('.claude/skill-authoring/om-verify/RULE_COVERAGE.yaml'))
t=pathlib.Path('.claude/skills/om-verify/SKILL.md').read_text(encoding='utf-8')
miss=[(r,x['contains']) for r,v in cov['rules'].items() if v['status'] in ('APPLY','TRANSFORM') for x in v['targets'] if x['contains'] not in t]
print('missing targets:', miss)
"

# 4) 참고 자료 저장소 무변경 (0)
git -C /Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/kb-datacatalog-upgrade-checker-om-plan-cli status --porcelain | wc -l

# 5) 이 저장소 변경 경로 (om-plan 경로는 타 세션 것)
git status --short
```

추가로, 이번 재빌드에 한해 다음 대조를 권한다. 작성 세션 실측 결과는 **`12/12` · `5/5` · `16/16`으로 전부 일치**한다.

```bash
# 영어본 ↔ 한글본 구조 계수 비교 (절·절차·경계 항목 수가 같아야 함)
python3 -c "
import pathlib,re
kr=pathlib.Path('.claude/skills/om-verify/SKILL.md').read_text(encoding='utf-8').splitlines()
en=pathlib.Path('.claude/skill-authoring/om-verify/SKILL_EN_PREVIOUS.md').read_text(encoding='utf-8').splitlines()
def c(ls,p): return sum(1 for l in ls if re.match(p,l))
print('절(##)    kr/en:', c(kr,r'^## '), '/', c(en,r'^## '))
print('절차(###) kr/en:', c(kr,r'^### '), '/', c(en,r'^### '))
print('표 행    kr/en:', c(kr,r'^\| '), '/', c(en,r'^\| '))
"
```

---

## 7. 실측 상태

| 항목 | 값 |
|---|---|
| `FINAL_STATUS.json` | `PASS`, `2026-08-25T01:46:17Z` |
| `VALIDATION_BUILD.json` | `ok: true`, errors 0, warnings 0 |
| `VALIDATION_AUDIT.json` | `ok: true`, `audit_verdict: PASS` |
| 규칙 판정 | 89 = `APPLY` 43 · `TRANSFORM` 22 · `EXCLUDE` 18 · `EXTERNAL` 6. 누락·중복·미판정 0 |
| 안티패턴 | 26 = `PASS` 21 · `NOT_APPLICABLE` 5 |
| 앵커 | 71개 전부 한글 본문에 실재 |
| 설계 요구 | 18/18 `RESOLVED` |
| 설계서 digest 사슬 | 3/3 일치 |
| 검사기 저장소 | `git status --porcelain` **0줄**, HEAD `82be68e733` 불변 |
| 정본 문서 4종 | 읽기만 수행. 저작 세션의 쓰기 명령 0건 (`24` 문서의 갱신 시각은 사람이 한글화 지시서를 작성한 시각과 같다) |
| 이 저장소 변경 | `.claude/skills/om-verify/`·`.claude/skill-authoring/om-verify/`·`docs/om-verify/`·이 보고 파일. **om-plan 경로는 타 세션 작업이라 손대지 않음** |
| 커밋 | **0건** |

---

## 8. 남은 위험 (전부 `AUTHORING_REVIEW.md`에 기록됨)

1. **영어 기준본 바이트 동일성 미증명** — §5. 이번 재빌드가 만든 유일한 새 위험이다.
2. **`UserPromptExpansion` 미배선** — 검사기 저장소 matcher가 `^(om-plan|om-resume)$`라 사용자가 직접 친 `/om-verify`는 훅 검사를 받지 않는다. `state: planned`로 정직 기록.
3. **wiring 테스트 단언 문구** — 이식 시 `harness/tests/test_claude_wiring.py`의 단언 대상이 한글 경계 문장이 된다(예: 「실행하고 판정하고 기록할 뿐이다」, 「어느 것도 통과가 아니다」, 「커밋·푸시·태그·배포를 하지 않는다」). 문구를 바꾸면 테스트가 깨진다. **Codex 작업 항목.**
4. **엔진 잔여 위험** — `gitprim.GitPrimitiveError`가 어느 예외 절에도 걸리지 않아 git 조회 실패 시 receipt 없이 종료코드 1이 날 수 있다. 스킬 결함이 아니라 검사기 후속 검토 항목.
5. **실행 검증 미수행** — 이 세션도, 감사자 6명도 `verify run`을 실제로 실행하지 않았다. 실행 근거는 `81`의 실데이터 리허설이다.
6. **`argument-hint` 값만 영어** — 사람 결정이 기술 토큰으로 지정한 값이다. 이식 시 사람 재검토 항목.
7. **이미 있는 요청 파일을 넘기는 경우의 서술 부재** — `argument-hint`는 `verify request path`도 받지만 본문은 이 명령이 요청 파일을 작성한다고만 쓴다. 6차 감사가 **영어 승인본에도 동일하게 있던 성질**로 판정했다. 다음 개정 항목.
8. `Write` 경로 무제한 / `cd && python` 결합 호출 미승인 / `retries` 본문 미나열 — 안전 영향 없는 기지 항목.
9. **정본 `81`의 남은 위험 6건 승계** — 구형 인계 호환 / 공식 Dockerfile 재현성 미검증 / local-issued 한계 / test-agent `meta.run_id` null / run ID 전역 유일성 미강제 / fixture 사후 변경 미증명.

---

## 9. 다음 단계

1. 사람 확인.
2. **Codex 2차 검토** — 이 문서 + `검토요청_om-verify_저작결과_20260825.md` + `검토결과_om-verify_1차_20260825.md`를 함께 전달.
3. 승인 후 검사기 저장소로 이식 + `test_claude_wiring.py`에 **한글** 문구 단언 추가 + om-apply 한글화(정본 `83` 작업3, Codex).
4. 커밋·푸시는 그 이후 사람 지시로. **현재까지 커밋 0건.**
