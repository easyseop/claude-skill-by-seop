# om-verify 확인 감사 — 실행기 전환 반영 (2026-08-25, 새 인스턴스, 읽기 전용)

(직전 6차 PASS본의 한정 수정(87 반영) 확인 감사. 감사자 반환 원문 그대로, 판정 무수정.)

- 판정: PASS
- 핵심 이유: 한정 수정 2갈래(실행 명령·allowed-tools `harness/om` 교체, 3단계 종료코드 2 설명에 실행기 중단 경우 추가)가 검사기 실물과 일치하고, 6차 PASS본의 줄 지도 대조 결과 수정 지점 밖 변형 0건.
  - 검사기 정합: :135-136 exit 2 문장은 래퍼의 두 실패 지점(.venv 버전 미달/탐색 실패) 모두 안내 후 exit 2 + om_workflow의 WorkflowInputError·argparse 2 반환과 일치. `harness/om verify run <REQUEST_JSON> --run-dir <NEW_RUN_DIR>`(:130)은 verify run 서브커맨드 인자와 정확 대응. exit 0/1/3·pre-run stderr analysis_error+3 서술은 무변경 그대로 코드와 일치.
  - allowed-tools: `Bash(harness/om verify run *)` 본문 명령과 문자 대응, 총 9항목 동일.
  - 의도 밖 변형 0: 197줄 동일, 6차 감사의 줄 지도(:7·:9·:25·:34-35·:42-43·:46·:52-56·:58·:72·:74·:93-94·:120·:135-138·:150-152·:169) 전 문장이 같은 줄 번호에 원문 잔존 — 변경이 15행·130행·135-136행의 제자리 치환에 국한됐다는 강한 증거. `python`·`om_workflow` 잔존 0건, 앵커 71건 전수 실재(실행기 관련 2건만 교체), VALIDATION_BUILD 재실행분 ok:true.

## 발견 (비차단 2 + 참고 1)
1. (증거 파일) AUTHORING_REVIEW 「남은 위험」 5번의 "`cd ... && python ...`" 문구 — 실행기 교체 후에는 "`cd ... && harness/om ...`"이 해당. 성질(결합 호출은 사전 승인 밖 → 권한 질문 증가, 안전 문제 없음)은 유효 — 다음 정비 시 문구 갱신 권고.
2. (기존 성질) 3단계의 cd 후 실행을 한 Bash로 결합하면 접두 매칭에 안 걸려 권한 질문 발생 — 이전 PASS본에도 있던 성질로 이번 수정이 만든 것 아님.
3. (참고) om_workflow.py:14-20의 자체 3.11 가드는 문서화된 경로(harness/om)에서는 래퍼가 먼저 걸러 도달 불가 — 본문의 두 경우 열거는 문서화된 경로에서 완전.

## 확인 범위와 미확인
- 직접: 수정본 전문·래퍼·om_workflow(verify 디스패치·exit·버전 가드)·87·AUTHORING_REVIEW·6차 원문(줄 지도)·앵커 71건 전수 대조·VALIDATION_BUILD.
- 미확인: 6차 시점 원본 파일 부재로 바이트 diff 불가(합성 증거 기반). verify run 실행·엔진 게이트 재검증은 수정 범위 밖(6차 실측 의존). 파일 무수정.
