# 검토 요청 — om-verify 스킬 저작 결과 (Claude 1차 검토용)

작성일: 2026-08-25. 작성자: Claude(저작 세션). 수신: **다른 Claude 세션(1차 검토자)**. 이후 Codex 2차 검토 예정.
저장소: `/Users/seop/claude-skill-by-seop` (브랜치 `main`, 미커밋 상태).
근거 지시서: `지시서_om-verify_스킬저작_20260824.md`(이 저장소 루트).

> 이 문서 하나만 읽고 검토를 시작할 수 있도록 작성했다. 결론·산출물 위치·재현 명령·미결 항목·집중 검토 지점이 모두 아래에 있다.

---

## 0. 검토자 행동 규칙 (먼저 읽을 것)

1. **읽기 전용으로 시작한다.** 수정이 필요하다고 판단되면 고치지 말고 지적 목록으로 반환한다. 저작자의 주장을 사실로 전제하지 않는다.
2. **다음 경로는 검토 대상이 아니다.** 다른 세션이 병렬로 `om-plan`을 저작 중이다: `.claude/skills/om-plan/`, `.claude/skill-authoring/om-plan/`, `docs/om-plan/`. 읽지도 판정하지도 않는다.
3. **참고 자료 저장소 2곳은 읽기 전용이다.** 어떤 쓰기도 금지한다(아래 §3).
4. **커밋·푸시·브랜치 변경 금지.** 저작 세션도 하지 않았다.
5. 이미 독립 감사가 4회 수행됐다(§6). 같은 항목을 반복하기보다 **§8의 집중 검토 지점**과 **§9의 알려진 약점**을 우선 검증한다.

---

## 1. 결론

- `FINAL_STATUS.json` = **`PASS`** (`2026-08-25T00:23:16Z`).
- 기계 검증 3단 전부 종료코드 0: `spec` / `build` / `audit`. `audit_verdict: PASS`.
- 독립 감사 4회(매회 새 감사자 인스턴스): 1차 PASS → 2차 PASS → 3차 CONDITIONAL → 4차 PASS(차단 0건).
- 규칙 89개 전수 판정: 누락 0 · 중복 0 · 미판정 0.
- 설계 요구사항 18개 전부 `RESOLVED`, `DESIGN_REQUIREMENTS.status: APPROVED`.
- 참고 자료 저장소 쓰기 0건, 커밋 0건.

---

## 2. 산출물 위치

### 런타임 스킬 (이식 대상)

| 경로 | 내용 |
|---|---|
| `.claude/skills/om-verify/SKILL.md` | 225줄, 12,146바이트. **이 폴더의 유일한 파일.** `references/`·`scripts/`·`assets/`·`evals/`는 만들지 않았다 |

### 설계서

| 경로 | 내용 |
|---|---|
| `docs/om-verify/OM_VERIFY_DESIGN.md` | 327줄. DESIGN_INPUT 템플릿 20개 절 전부 작성. 빈 항목 없음 |

### 작성·감사 증거 (14개, `.claude/skill-authoring/om-verify/`)

| 파일 | 내용 |
|---|---|
| `DESIGN_REQUIREMENTS.yaml` | 필수 18항 전부 `RESOLVED` + `sources`. `status: APPROVED` |
| `RULE_MANIFEST.yaml` | 원문에서 자동 추출한 규칙 89 · 안티패턴 26 · stage_modules 9 |
| `COMMAND_SPEC.yaml` | 실행 계약. `status: APPROVED` |
| `RULE_COVERAGE.yaml` | 89개 전수 판정 + 안티패턴 26 + stage_modules |
| `SPEC_REVIEW.md` | 66줄. 조사 범위·단계 판정·권한 결정·frontmatter 결정 |
| `AUTHORING_REVIEW.md` | 265줄. 설계 추적·규칙 집계·반영 위치·문장별 검토·감사 반영·남은 위험 |
| `AUDIT_REPORT_ROUND1.md` | 1차 감사 원문 (PASS) |
| `AUDIT_REPORT_ROUND2.md` | 2차 감사 원문 (PASS) |
| `AUDIT_REPORT_ROUND3.md` | 3차 감사 원문 (**CONDITIONAL**) |
| `AUDIT_REPORT.md` | 4차·최종 감사 원문 (PASS) |
| `VALIDATION_SPEC.json` | `ok: true`, `2026-08-24T13:14:21Z` |
| `VALIDATION_BUILD.json` | `ok: true`, `2026-08-25T00:23:16Z` |
| `VALIDATION_AUDIT.json` | `ok: true`, `audit_verdict: PASS`, `2026-08-25T00:23:16Z` |
| `FINAL_STATUS.json` | `status: PASS` |

---

## 3. 참고 자료 (읽기 전용 — 쓰기 금지)

- **검사기 저장소(대상 런타임)**: `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/kb-datacatalog-upgrade-checker-om-plan-cli/`
  - 저작 시각 실측: HEAD `82be68e733`, 브랜치 `codex/om-plan-verified-gates-20260820`, `git status --porcelain` 0줄.
  - CLI: `harness/om_workflow.py` / 엔진: `harness/acgh/verifycore/{workflow,testruns,pytest_runs,result_io,schema}.py` / OM 어댑터: `harness/acgh/integrations/om/verify.py`
  - I/O 스키마 5종: `harness/acgh/verifycore/schema/`
  - 반례 테스트: `harness/tests/test_om_verify_counterexamples.py` / wiring: `harness/tests/test_claude_wiring.py`
  - 권한·훅: `.claude/settings.json` / 형식 기준 스킬: `.claude/skills/om-apply/SKILL.md` / 규칙: `CLAUDE.md`
- **정본 문서**: `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/skill_develop/om_plan/`
  - `83_Codex_SKILLmd_작성지시_20260824.md` (부록 A = 내용 요구 정본)
  - `76_omverify_구현지시서_20260824.md` (경계 규율)
  - `81_Claude_omverify_리허설_재검증_20260824.md` (실데이터 통과 증거 + 남은 위험 6건)
  - `24_누락감사_사람결정과_기록_20260820.md` (사람 결정 기록)
- **메타스킬 원문 규칙 2종**: `.claude/skills/claude-skill-author/references/01_...md`, `02_...md`

---

## 4. 스킬 프로필 (확정값)

| 항목 | 값 |
|---|---|
| 명령 | `/om-verify` |
| 주단계 / 보조단계 | `V`(검증·리뷰) / `R`(조사·이해), `D`(문서화·보고) |
| operation | `new` (저작·대상 저장소 양쪽에 동명 스킬 부재 실측) |
| 호출 정책 | 수동 전용 — `disable-model-invocation: true` |
| 실행 컨텍스트 | inline, `background: false`, 서브에이전트 없음 |
| 부작용 등급 | 중간 — 새 run 디렉터리 생성·계약 테스트 실행·docker/HTTP 조회. 코드·관리파일·등록자료·기존 receipt 불변 |
| frontmatter | `name`·`description`·`argument-hint`·`disable-model-invocation`·`allowed-tools` 5개만 |
| `allowed-tools` | 9항목 — `Read`·`Glob`·`Grep`·`Write` + `Bash(python harness/om_workflow.py verify run *)`·`Bash(git status)`·`Bash(git status *)`·`Bash(git diff)`·`Bash(git diff *)`. `Edit`·`Bash(*)` 미포함 |
| 배포 대상 | Claude Code 전용 (`claude --version` = 2.1.241 실측) |
| 본문 언어 | 영어 (사유는 §9-1) |

---

## 5. 규칙 판정 집계

| 구분 | 값 |
|---|---|
| 원문 규칙 | 89 (C 20 · P 8 · D 12 · E 7 · V 8 · S 7 · M 5 · CC 22) |
| 판정 | 89 — `APPLY` 42 · `TRANSFORM` 23 · `EXCLUDE` 18 · `EXTERNAL` 6 |
| 누락 / 중복 / 미판정 | 0 / 0 / 0 |
| 안티패턴 | 26 — `PASS` 21 · `NOT_APPLICABLE` 5 (A-CC-05·06·07·08·14) |
| `APPLY`·`TRANSFORM` 반영 위치 | 65개 규칙 → 70개 `targets[].contains`, 전부 `SKILL.md`에 실재 |
| `EXTERNAL` 통제 | 6건 / `required_for_pass: true` 5경로 전부 구현 확인 / `planned` 1건(§9-2) |

`EXTERNAL` 6건이 지목한 기술적 통제(전부 검사기 저장소 실물):

- `harness/om_workflow.py` — verify 하위명령의 결정적 실행 (D-09)
- `harness/acgh/verifycore/workflow.py` — fail-closed 게이트·`VERIFY_RUN_ALREADY_EXISTS`·`controlled_names` (V-05)
- `harness/acgh/verifycore/pytest_runs.py` — `_BLOCKED_ENV` 6종 (V-05)
- `harness/acgh/verifycore/result_io.py` — `os.chmod(destination, 0o444)` (S-07)
- `.claude/settings.json` — `permissions.deny`의 `git push`·`git tag`·`docker push`·`kubectl apply`·`helm upgrade` (S-01·S-07·CC-07)
- `.claude/hooks/run_om_plan_hook.sh` — `PreToolUse` 훅 (CC-07·CC-21, `required_for_pass: false`)

---

## 6. 독립 감사 이력

| 회차 | 판정 | 차단 | 비차단 | 처리 | 원문 |
|---|---|---|---|---|---|
| 1차 | PASS | 0 | 11 | 9건 반영, 2건 사유 기록 후 미반영 | `AUDIT_REPORT_ROUND1.md` |
| 2차 | PASS | 0 | 12 | 9건 반영, 2건 미반영, 1건 확인만으로 해소 | `AUDIT_REPORT_ROUND2.md` |
| 3차 | **CONDITIONAL** | 1 | 9 | 차단 1건 해소 + 비차단 8건 반영 | `AUDIT_REPORT_ROUND3.md` |
| 4차 | PASS | 0 | 7 | 4건 반영, 3건 남은 위험으로 고지 | `AUDIT_REPORT.md` |

감사자는 매회 `skill-author-auditor` **새 인스턴스**(도구 `Read, Glob, Grep`, `permissionMode: plan`)를 사용했고 파일을 수정하지 않았다.

### 3차 CONDITIONAL의 원인과 해소

- **원인**: `AUTHORING_REVIEW.md`와 `AUDIT_REPORT_ROUND1.md`이 2차 감사 판정의 소재지로 `AUDIT_REPORT.md`를 인용했으나, 당시 그 파일은 rubric 템플릿이었고 2차 원문이 어디에도 없었다. 저작자가 2차 감사 반환 원문을 파일로 남기지 않은 기록 누락이다. **스킬 자체의 결함이 아니다**(3차 감사자도 "`SKILL.md` 수정 불필요"로 판정).
- **해소**: 2차 원문을 `AUDIT_REPORT_ROUND2.md`에, 3차 원문을 `AUDIT_REPORT_ROUND3.md`에, 4차 원문을 `AUDIT_REPORT.md`에 기록하고 인용 경로를 실물에 맞췄다. 4차 감사자가 해소를 확인했다.

### 감사가 잡아낸 실제 정확도 결함 (반영 완료)

1. **`exit 2` 의미 오기** — "요청이 거부되면 exit 2"라고 적었으나 실측은 exit **3**. `plancore/schema.py`의 `read_data`가 읽기 실패를 `PlanControlError(INPUT_UNREADABLE)`로 올리고 `om_workflow.py`의 `main`이 이를 `analysis_error` + exit 3으로 매핑한다. exit 2는 argparse 오류·`WorkflowInputError`·Python 3.11 미만뿐이다. → 지시서 §5-4의 "코드 실측, 추측 금지" 위반이었으므로 정정했다.
2. **fixture receipt 과잉 단언** — 6필드 전부 대조된다고 적었으나 `integrations/om/verify.py`의 `inspect_runtime`이 실제 대조하는 것은 `run_id`·`candidate_sha`·`container_id`·`volume_names` 4개뿐이다. `fixture_set_digest`는 기록만, `applied_at`은 미사용. → "recorded but not cross-checked, so never report them as bound"로 분리 표기했다.
3. **`not_configured` 분기와의 문면 충돌** — "실행되지 않은 게이트는 통과가 아니다"가 `ui_component` 부재 시 `status: verified` + `execution_status: not_configured`로 기록되는 엔진 동작과 모순이었다. → 대상을 `skipped_due_to_stop`으로 좁혔다. 2·3·4차 감사자가 이 좁힘이 완화(거짓 verified 허용)가 아님을 코드로 각각 확인했다.
4. **`AUTHORING_REVIEW`의 줄 번호·계수 오류 반복** — 1·2·3차가 연속 지적. → 문서 생성 시 `SKILL.md`에서 문구를 검색해 줄 번호를 계산하도록 바꿔 수기 표기를 제거했다. 4차 감사자가 30개 이상 표본 대조로 확인했다.

---

## 7. 재현 명령

검토자가 직접 확인할 수 있는 명령이다. 전부 읽기 전용이거나 증거 폴더에만 쓴다.

```bash
cd /Users/seop/claude-skill-by-seop

# 1) 기계 검증 3단 (각각 종료코드 0이어야 함)
python3 .claude/skills/claude-skill-author/scripts/validate_authoring.py \
  --project-root "$(pwd)" --target om-verify --phase spec
python3 .claude/skills/claude-skill-author/scripts/validate_authoring.py \
  --project-root "$(pwd)" --target om-verify --phase build
python3 .claude/skills/claude-skill-author/scripts/validate_authoring.py \
  --project-root "$(pwd)" --target om-verify --phase audit

# 2) 설계서 digest 사슬 (3건 모두 True 여야 함)
python3 -c "
import json,hashlib,pathlib
d=json.load(open('.claude/skill-authoring/om-verify/DESIGN_REQUIREMENTS.yaml'))
for x in d['source_documents']:
    print(hashlib.sha256(pathlib.Path(x['path']).read_bytes()).hexdigest()==x['sha256'], pathlib.Path(x['path']).name)
"

# 3) APPLY/TRANSFORM target 실재 (누락 0이어야 함)
python3 -c "
import json,pathlib
cov=json.load(open('.claude/skill-authoring/om-verify/RULE_COVERAGE.yaml'))
t=pathlib.Path('.claude/skills/om-verify/SKILL.md').read_text(encoding='utf-8')
miss=[(r,x['contains']) for r,v in cov['rules'].items() if v['status'] in ('APPLY','TRANSFORM') for x in v['targets'] if x['contains'] not in t]
print('missing targets:', miss)
"

# 4) 참고 자료 저장소 무변경 (0 이어야 함)
git -C /Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/kb-datacatalog-upgrade-checker-om-plan-cli status --porcelain | wc -l

# 5) 이 저장소 변경 경로 (om-plan 경로는 타 세션 것)
git status --short
```

`validate_authoring.py`는 `RULE_COVERAGE.yaml`의 `summary`와 `VALIDATION_*.json`을 갱신하므로 증거 폴더에 쓰기가 발생한다. 그 외 쓰기는 없다.

---

## 8. 집중 검토 지점 (여기부터 보라)

감사 4회가 이미 규칙 전수 판정·target 실재·외부 통제 실재를 확인했다. 1차 검토자는 아래를 우선한다.

1. **코드 실측 주장의 재반증.** `SKILL.md`가 단정하는 사실을 검사기 코드로 다시 깨보라. 특히:
   - 종료코드: `verified` 0 / `failed` 1 / `infra_error` 3, exit 2의 범위, pre-run 차단의 `analysis_error` + 3
   - 게이트 4종 이름과 `_final_status`가 최악 게이트를 택한다는 주장
   - waiver가 `VERIFY_REQUIRED_TEST_FAILED:` 사유만 제거한다는 주장
   - 통제 환경변수 2종(`OPENMETADATA_BASE_URL`·`OPENMETADATA_PRODUCT_REPO`)
   - 함정 표 4행의 reason code 이름과 발생 기전
2. **완료 정의의 안전성.** `SKILL.md`의 "no gate stopped with `skipped_due_to_stop`"이 거짓 완료를 허용하는 조합이 있는지. 2·3·4차가 각각 확인했으나 가장 위험한 문장이므로 재확인 가치가 있다.
3. **83 부록 A 충족도.** A-1(모든 I/O에 스키마 경로 명시) · A-2(호출마다 4단 체크) · A-3(케이스별 지시 + 대표 거부 4종) · A-4(근거 규율)를 `SKILL.md`가 실제로 만족하는지. 정본 문서를 직접 열어 대조하라 — **1차 감사자만 `83`을 읽었고 2·3·4차는 읽지 않았으며, `76`·`81`·`24`는 4회 모두 아무도 열지 않았다**(§9-5). 부록 A 충족도는 감사 4회 중 1회만 확인된 항목이다.
4. **이식 적합성.** 이 스킬은 검사기 저장소로 옮겨진다. `om-apply/SKILL.md`와 형식·어조·경계 표현이 일관되는지, `test_claude_wiring.py`에 추가할 문구 단언이 안정적인지.
5. **역방향 감사.** `SKILL.md` 225줄에 근거 없는 문장·중복·상충이 남았는지.

---

## 9. 알려진 약점과 미결 (은폐 없음 — 전부 증거에 기록됨)

1. **`D-01` 언어 규정 충돌 (유일하게 열린 설계 판단).**
   규칙 `D-01`은 "규칙 문장은 한글로 작성한다"를 요구한다. 그러나 이식 대상 저장소의 `.claude/skills/om-apply/SKILL.md`가 영어이고, `harness/tests/test_claude_wiring.py`가 영어 문구를 단언하며, 정본 `83` 작업3이 om-verify에도 같은 방식("never 계열 문구")의 단언을 요구한다. **런타임 본문은 영어, 저작 증거·설계서는 한글**로 결정했다. 임의 결정이 아니라 저장소 사실 기반 결정이며 `RULE_COVERAGE.D-01.rationale`·`SPEC_REVIEW.md`·`AUTHORING_REVIEW.md`·`COMMAND_SPEC.open_questions` 네 곳에 기록했다. **사람이 뒤집을 수 있는 항목이다.**
2. **미구현 외부 통제 1건 — `UserPromptExpansion` (CC-21).**
   검사기 저장소 `.claude/settings.json`의 matcher가 `^(om-plan|om-resume)$`이라 사용자가 직접 입력한 `/om-verify`는 이 훅을 거치지 않는다. `state: planned` · `required_for_pass: false`로 기록했고 구현했다고 적지 않았다. 이식 시 matcher 확장 여부는 사람 결정 사항이다.
3. **엔진 잔여 위험 (스킬 결함 아님, 검사기 후속 검토 대상).**
   `acgh/gitprim.py`의 `GitPrimitiveError`는 `RuntimeError`라서 `run_verify`의 예외 절(`PlanControlError, OSError, KeyError, TypeError, ValueError`)에도 `om_workflow.py:main`의 세 예외 절에도 걸리지 않는다. 후보 저장소의 git 조회가 실패하면 receipt 없이 traceback과 함께 종료코드 1이 날 수 있어 `SKILL.md`의 "exit 1 is failed"와 겉보기가 겹친다. 4단계가 receipt 읽기를 요구하므로 오판정으로 이어지지는 않는다. 3차 감사자가 발견하고 4차 감사자가 재현 확인했다.
4. **실행 검증 미수행.**
   이 저작 세션은 문서를 만들었을 뿐 `verify run`을 실제로 실행하지 않았다. 실행 근거는 `81`(실데이터 리허설)의 실측치이며 이 세션이 새로 실행해 확인한 것이 아니다. 감사자 4명도 실행 검증을 하지 않았다.
5. **감사자의 공통 미확인 사항.**
   - 4개 세션 모두 `Grep`·`Glob`이 `ENOEXEC`로 실패해 파일 열거 없이 `Read` 전문 읽기로 대조했다. 보조 파일 부재는 표준 4경로(`references`·`scripts`·`assets`·`evals`) 확인으로 대체했다. 저작 세션이 `ls`로 `SKILL.md` 단일 파일임을 확인했다.
   - 감사자에게 셸 실행 수단이 없어 sha256·`claude --version`·`git status`를 독립 재확인하지 못했다(저작 세션이 §7 명령으로 확인).
   - 정본 문서 열람이 회차별로 갈린다: **1차만 `83`을 읽었고**(1차 「감사 범위」에 명시) 2·3·4차는 `83`을 열지 않았다. `76`·`81`·`24`는 **4회 모두 열리지 않았다**. 따라서 부록 A 충족도(§8-3)와 `76`의 경계 규율 반영 여부는 감사 4회로 충분히 검증됐다고 볼 수 없다. **이번 검토의 핵심 공백이다.**
6. **잔여 소소 항목 3건 (안전 영향 없음).**
   - `Write`가 경로 무제한 — run·요청 파일 경로가 실행마다 달라 고정 패턴으로 좁히지 못했다. 사유를 `COMMAND_SPEC.permissions.allowed_tools_policy`에 기록.
   - `allowed-tools`가 `cd ... && python ...` 단일 호출 형태를 사전 승인하지 않는다 — 본문이 `cd`와 명령을 두 줄로 제시한다. 제한이 아니라 사전 승인이므로 권한 질문 빈도만 증가.
   - `retries`(스키마상 `const: 0`)를 입력 계약의 선택 필드 목록에 나열하지 않고 경계 문장에서만 다룬다. 4차 감사 지적이며 한 단어 추가로 해소 가능하다. 4차가 검증한 `SKILL.md`를 마감 후 다시 고치지 않기로 해 미반영으로 남겼다.
7. **3번째 호출 예는 합성 예시.**
   지시서가 준 호출 예는 2건이고 3번째(`/om-verify run-dir 재사용으로 막혔는데 이게 고장인지 차단인지 봐줘`)는 설계서 §2의 사용 시점에서 파생했다. 규칙 `P-01`의 "실제 사용자 요청 3개 수집"을 문면대로 충족하지는 않는다. 설계서 §4에 파생 근거를 명시했다.
8. **정본 `81`의 남은 위험 6건 승계.**
   구형 인계 호환 / 공식 Dockerfile 재현성 미검증(`Dockerfile.local-base` 우회) / local-issued 한계(보호된 CI 발행은 후속) / test-agent `meta.run_id` null로 UI 부품 미결속 / run ID 전역 유일성 미강제 / fixture 사후 변경 미증명(§6-2의 "기록 전용 2필드" 표기와 연결).
9. **임시 결정 R-6.**
   V-1~V-3(환경 C안 · UI 핵심부터 · 콘솔 기본 fail)은 실물 운용 후 재검토 대상이다(`24` 재검토표).

---

## 10. 검토 후 예정 경로

1. **Claude 1차 검토(이 문서 수신자)** → 지적 목록 반환.
2. 지적 반영 여부를 사람이 결정.
3. Codex 2차 검토.
4. 사람 승인 후 검사기 저장소 `.claude/skills/om-verify/SKILL.md`로 이식 + `harness/tests/test_claude_wiring.py`에 문구 단언 추가(정본 `83` 작업3).
5. 커밋·푸시는 그 이후 사람 지시로 수행. **현재까지 커밋 0건.**

---

## 11. 반환 형식 (1차 검토자용)

```markdown
# om-verify 저작 결과 — Claude 1차 검토 결과

## 종합 판정
- 판정: 승인 | 조건부 승인 | 반려
- 핵심 이유:

## 차단 문제
| 심각도 | 위치 | 문제 | 근거(파일:줄) | 수정 방향 |
|---|---|---|---|---|

## 코드 실측 재반증 결과
| 검토자가 깨보려 한 주장 | 결과 | 근거(파일:줄) |
|---|---|---|

## 83 부록 A 충족도
| 항목 | 충족 | 근거 |
|---|---|---|
| A-1 I/O 스키마 경로 명시 | | |
| A-2 호출별 4단 체크 | | |
| A-3 케이스별 지시·대표 거부 4종 | | |
| A-4 근거 규율 | | |

## 비차단 개선사항

## 검토 범위와 미확인 사항
```
