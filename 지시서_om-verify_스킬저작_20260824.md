# om-verify 스킬 저작 지시서 (원스톱 지시서의 작업 대상 정보 교체판 2)

> `지시서_om-plan_스킬저작_20260824.md`와 같은 방식. **om-plan 저작을 먼저 끝낸 뒤** 이 문서로 om-verify를 저작한다(메타스킬은 스킬 하나당 한 번의 full 절차·별도 증거 디렉터리). 원본 절차는 `CLAUDE_원스톱_설치_설계_스킬생성_지시서_omplan_v1.1.md`를 따른다. 설치는 완료 상태.

## 작업 대상 정보 (교체)

- 대상 스킬: `om-verify`
- 호출 이름: `/om-verify`
- 설계서: `docs/om-verify/OM_VERIFY_DESIGN.md` (없으면 템플릿으로 생성)
- 최종 스킬: `.claude/skills/om-verify/SKILL.md`
- 작성·감사 증거: `.claude/skill-authoring/om-verify/`
- 최종 목적지(참고): 검토 후 검사기 저장소 `.claude/skills/om-verify/`로 이식되어 MR 합류. 이 저장소에서는 저작·감사만.

## 참고 자료 (교체 — 전부 실재 경로)

- 검사기 실물: `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/kb-datacatalog-upgrade-checker-om-plan-cli/`
  - CLI: `harness/om_workflow.py` (`verify run REQUEST --run-dir NEW_RUN_DIR`)
  - I/O 스키마(정본 계약): `harness/acgh/verifycore/schema/*.schema.json` 5종(request·build-receipt·fixture-receipt·ui-component·waiver)
  - 엔진: `harness/acgh/verifycore/` (workflow.py의 게이트·reason code), `harness/acgh/integrations/om/verify.py`
  - 실물 스킬 패턴: `.claude/skills/om-apply/SKILL.md`
- 요구·경계 정의: `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/skill_develop/om_plan/` 아래
  - `83_Codex_SKILLmd_작성지시_20260824.md` **부록 A** (verify 행·verify run 4단 체크·케이스별 지시 — 내용 요구 정본)
  - `76_omverify_구현지시서_20260824.md` (경계 규율 원본)
  - `81_Claude_omverify_리허설_재검증_20260824.md` (실데이터 통과 증거·남은위험 6건)
  - `24_누락감사_사람결정과_기록_20260820.md` (결정 기록)

## 추가 설계 요구 (필수)

1. verify는 **코드·관리파일을 절대 바꾸지 않는다** — 실행·판정·기록만.
2. 최종 상태는 `verified(0)`/`failed(1)`/`infra_error(3)` 셋뿐. **skip·부분실행·WARN·실행불가는 어느 것도 통과가 아니다.** exit 재해석 금지.
3. 선행물 없이는 시작 불가: apply의 `apply-result.json` 전체 인계 + build receipt(local-issued·source_clean) + fixture receipt + 떠 있는 서버(container_id 실측).
4. run-dir은 항상 새로 만든다(재사용은 `VERIFY_RUN_ALREADY_EXISTS`로 거부됨 — 고장이 아니라 차단). receipt는 읽기전용, retries=0.
5. 테스트 목록은 등록 계약에서 재계산되며 인계값과 다르면 중단. 대표 거부 4종(다른 서버·등록자료 불일치·전부 skip·run-dir 재사용)을 "차단이 작동한 것"으로 설명.
6. receipt의 `trust_limitation` 문구(digest는 무결성이지 발행자 진위 아님)를 사람 보고에 그대로 전달. infra_error 3회 반복 시 사람 escalation.
7. UI 부품(test-agent)은 표시 일치 증거로 한정, report `meta.run_id` 계약 충족 시에만 결속.
8. 89규칙 전수판정의 대상 저장소는 검사기 저장소임을 전제로 판정.

## 완료 후

om-plan과 동일: 커밋하지 말고 사용자에게 보고 → Codex 검토 → 검토 통과 후 검사기 저장소 이식.
