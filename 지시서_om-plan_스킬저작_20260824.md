# om-plan 스킬 저작 지시서 (원스톱 지시서의 작업 대상 정보 교체판)

> 새 세션은 이 파일 + `CLAUDE_원스톱_설치_설계_스킬생성_지시서_omplan_v1.1.md`를 함께 읽는다.
> **원본 지시서의 절차(설치 점검→설계서→spec→작성→build→독립감사→FINAL_STATUS)는 그대로 따르되, 아래가 원본의 "작업 대상 정보"·"참고 자료"를 대체한다.** 설치는 이미 완료됨(재설치 불필요 — `.claude/skills/claude-skill-author/` 실재).

## 작업 대상 정보 (교체)

- 대상 스킬: `om-plan`  ← 원본의 omplan은 예시였음. 우리 파이프라인 명명(om-apply와 동일 형식)으로 통일.
- 호출 이름: `/om-plan`
- 설계서: `docs/om-plan/OM_PLAN_DESIGN.md` (이 저장소 안, 없으면 템플릿으로 생성)
- 최종 스킬: `.claude/skills/om-plan/SKILL.md`
- 작성·감사 증거: `.claude/skill-authoring/om-plan/`
- **최종 목적지(참고)**: 이 산출물은 검토 후 검사기 저장소 `.claude/skills/om-plan/`으로 옮겨져 MR에 합류한다. 이 저장소에서는 저작·감사만 한다.

## 참고 자료 (교체 — 전부 실재 경로, 절대경로로 읽을 것)

정본(충돌 시 이쪽이 우선):

- 검사기 실물: `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/kb-datacatalog-upgrade-checker-om-plan-cli/`
  - CLI: `harness/om_workflow.py` (plan start/check/plan-session-start/plan-preflight/plan-validate/plan-resume)
  - I/O 스키마(정본 계약): `harness/acgh/plancore/schema/*.schema.json` 5종
  - 실물 스킬 패턴: `.claude/skills/om-apply/SKILL.md`
  - 저장소 안내: `CLAUDE.md`, `README.md`
- 요구·경계 정의: `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/skill_develop/om_plan/83_Codex_SKILLmd_작성지시_20260824.md` (특히 부록 A: I/O 정본·4단 호출 체크·케이스별 지시 — **SKILL.md 내용 요구사항의 정본**)
- 결정 기록: `.../skill_develop/om_plan/24_누락감사_사람결정과_기록_20260820.md`
- 배경 요약: `.../skill_develop/공유정리/하위_om-plan_논의정리_20260821.md`

## 추가 설계 요구 (필수)

1. `/om-plan`은 **문서만 쓰는 스킬이 아니라 검사기 CLI를 호출하고 그 판정을 따르는 스킬**이다. 모든 판정은 결정적 검사기(exit 0/1/2/3)가 소유하며 LLM은 재해석 금지.
2. 4모드(initial/feature/change/upgrade)별 입력·산출·대표 실패 게이트를 83 부록 A-3대로 표로 포함.
3. 호출 단계는 83 부록 A-2의 4단 형식([호출 전 확인→호출→성공 판정→실패 시])을 따른다.
4. 결과는 제안일 뿐 — 승인은 사람(intent_review)·검사기의 몫. 등록자료·관리파일 수정 금지, 커밋·push 금지.
5. 89규칙 전수판정 시 대상 저장소는 이 저장소가 아니라 **검사기 저장소**임을 전제로 판정한다.

## 완료 후

- 결과(SKILL.md + AUTHORING_REVIEW.md + FINAL_STATUS)를 커밋하지 말고 사용자에게 보고 → 사용자가 Codex 검토로 전달(검토 통과 후 검사기 저장소 이식은 별도 세션).
