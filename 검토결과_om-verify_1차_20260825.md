# om-verify 저작 결과 — Claude 1차 검토 결과

검토일: 2026-08-25. 검토자: Claude(파이프라인 검증 세션 — om-verify 엔진의 구현지시서 76 작성·리허설 재검증 81 수행 세션과 동일). 방식: 재현 명령 전부 재실행 + SKILL.md의 코드 실측 주장을 검사기 코드로 독립 재반증 + 정본(83 부록 A·76·81) 직접 대조 — **감사 4회가 안 본 공백(§8-3·§9-5)을 집중 검증**.

## 종합 판정

- **판정: 승인** (차단 0건)
- 핵심 이유: 재현 명령 5종 전부 주장대로 재현됨(기계검증 3단 exit 0, digest 사슬 3/3, target 누락 0, 참고 저장소 무변경). 코드 실측 주장 14건을 독립 재반증한 결과 **전부 실제 코드와 일치**. 감사가 1회만 본 "83 부록 A 충족도"와 한 번도 안 본 76·81 경계는 이 검토에서 직접 대조해 충족 확인.

## 차단 문제

없음.

## 코드 실측 재반증 결과 (전부 검토자가 직접 실행·열람)

| 깨보려 한 주장 | 결과 | 근거(파일:줄) |
|---|---|---|
| 게이트 4종 이름 | ✅ 일치 | verifycore/workflow.py:307·324·406·414 |
| 최종 상태 = 최악 게이트 | ✅ `_final_status`=rank max | workflow.py:236-237 |
| waiver는 `VERIFY_REQUIRED_TEST_FAILED:` 사유만 제거, infra_error 불가침 | ✅ status=="failed"일 때만, prefix 필터 | workflow.py:381-389 |
| env 2종 주입 차단 + 스스로 설정 | ✅ `VERIFY_TEST_ENVIRONMENT_OVERRIDE` | workflow.py:338-343·359-360 |
| exit 0/1/3 + exit 2 범위 + pre-run 차단 `analysis_error`+3 | ✅ (아래 비차단 2 참조) | workflow.py:439, om_workflow.py:512-527 |
| escalation = 3회째 infra_error | ✅ `prior+1>=3` | workflow.py:440 |
| 인계 자격 3필드 | ✅ verdict/final_state/eligible 3중 AND | workflow.py:114-121 |
| receipt 0444 읽기전용 | ✅ `os.chmod(0o444)` | result_io.py:63 |
| fresh 모드 run id 격리 강제 | ✅ `VERIFY_FRESH_RUNTIME_NOT_ISOLATED` | om/verify.py:196-198 |
| timeout 기본 300·prior 기본 0 | ✅ | workflow.py:100·367·431 |
| UI `meta.run_id` 결속·WARN/flaky/부분 → 실패 | ✅ `VERIFY_UI_RUN_ID_MISMATCH`·`VERIFY_UI_WARN_OR_FAIL` | om/verify.py:282-289(상대행 34-41) |
| ui 부재 시 `not_configured`이며 verified 저해 없음 / `skipped_due_to_stop`은 항상 infra_error 동반 → "no gate skipped" 완료 정의는 중복이되 안전 | ✅ 거짓 완료 조합 없음 | workflow.py:329·411·414·428 |
| 함정 4종 reason code | ✅ 전부 실재 (2건은 리허설 receipt 실물로도 확인됨 — 81) | workflow.py·om/verify.py·testruns.py:106 |
| docker 실패가 exit 2로 새지 않음 | ✅ `check=False`→PlanControlError(3) | om/verify.py:34-45 |

## 83 부록 A 충족도 (감사 4회 중 1회만 확인됐던 공백 — 직접 대조)

| 항목 | 충족 | 근거 |
|---|---|---|
| A-1 I/O 스키마 경로 명시 | ✅ | Input contract 표가 6개 입력 전부에 스키마 경로 명시(SKILL.md:83-94), 필드는 스키마 실측과 일치(required 4종 등) |
| A-2 호출별 4단 체크 | ✅ | Procedure 5단계 전부 Input/Do/Output/Verify/If-it-fails 구조(:117-189) |
| A-3 케이스별 지시·대표 거부 4종 | ✅ | Case guide 3상태 표(:193-197) + Known traps 4행(:204-209) + "A rejection here is the block working" 프레임(:201) |
| A-4 근거 규율 | ✅ | 위 재반증 표 — 표본이 아니라 실측 주장 전수에 가깝게 깨봤고 반례 0 |

76 경계 규율·81 승계(trust_limitation 문구 전달 :185-186, retries=0 :54, 재계산 불일치 시 중단 :51-53, run-dir 재사용 금지 :55-56, escalation :69-70)도 전부 본문에 실재.

## 비차단 개선사항 (수정 여부는 사람/Codex 단계 결정)

1. **함정 표의 reason code 표기** — 실제 방출 문자열은 `VERIFY_REQUIRED_TEST_NOT_PASS:<test_id>=<outcome>`(testruns.py:106)로 접두사 형태. 표는 접두사만 적음 — 오해 소지 낮으나 "(prefix)" 한 단어를 붙이면 정확.
2. **exit 2 서술의 이론적 구멍** — `om_workflow.py:515-517`이 `subprocess.CalledProcessError`→exit 2("Git 명령 실행 실패")를 매핑. verify 경로에서는 도달 불가 확인(git 오류는 GitPrimitiveError로 변환·docker는 check=False)이므로 본문 수정 불요. 단 §9-3(GitPrimitiveError 미포착→traceback exit 1)과 함께 검사기 후속 정비 목록에 남길 것.
3. §9-6의 기지 3건(Write 무제한·cd&&python 사전승인 부재·retries 미나열) — 동의, 이식 시 한 단어 추가(retries)만 권장.

## 사람 결정 필요 항목 (검토자가 확정하지 않음)

- **D-01 언어(본문 영어)**: 근거(이식 대상 일관성·wiring 영어 단언) 타당 — 유지 권고하되 사람 확인 요.
- **UserPromptExpansion matcher에 om-verify 추가 여부**(§9-2): 이식 시 결정.

## 검토 범위와 미확인 사항

- 이 검토도 `verify run` 실행은 하지 않음(§9-4와 동일) — 실행 근거는 81의 실데이터 리허설(별도 세션에서 receipt digest 재계산까지 완료)에 있음.
- 감사 증거의 내용 전수(예: RULE_COVERAGE 89건 각각의 판정 타당성)는 재검토하지 않음 — 기계 검증(전수·중복·누락 0)과 4회 감사를 신뢰 기반으로 채택, 내용 검증은 위 재반증·정본 대조로 갈음.
- **다음 = Codex 2차 검토** (구현자 관점 적대 검토): 이 결과와 검토요청서를 함께 전달.
